
-- 创建用户提到的账号（如果是真实账号）
-- 但实际上用户可能输入错误，已有的账号是：
-- jygxb2464@163.com 和 jygxb2464@126.com

-- 如果需要创建新的管理员账号，取消下面的注释
-- 注意：这里我不直接创建，因为可能是用户输入错误

-- 修复：确保所有national_agent都有正确的权限
UPDATE profiles 
SET 
  membership_type = 'national_agent',
  membership_expires_at = '2099-12-31 23:59:59+00',
  updated_at = NOW()
WHERE membership_type = 'national_agent' 
  OR email IN ('jygxb2464@163.com', 'jygxb2464@126.com');

-- 添加注释说明
COMMENT ON COLUMN profiles.membership_type IS '会员类型: basic(普通会员), vip(VIP会员), city_agent(城市代理), national_agent(全国代理/管理员)';
